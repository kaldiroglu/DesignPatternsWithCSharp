namespace dev.kaldiroglu.Adapter.Pluggable.Parameterized;

/// <summary>
/// <b>Adaptee</b> #3: a Kenyan source with its own, incompatible interface (<c>hakunaMatata</c>).
/// </summary>
public sealed class KenyaPowerSource
{
    public void HakunaMatata()
    {
        Console.WriteLine("Kenyan source: switch is pushed in a hakuna matata way :)");
    }
}
