<Project>

  <!-- Common settings for every project in the Flyweight C# solution. -->
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>
    <RootNamespace>dev.kaldiroglu.Flyweight</RootNamespace>
  </PropertyGroup>

</Project>
