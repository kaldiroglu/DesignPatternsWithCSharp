<Project>

  <!-- Common settings for every project in the Composite C# solution. -->
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>
    <RootNamespace>dev.kaldiroglu.Composite</RootNamespace>
  </PropertyGroup>

</Project>
